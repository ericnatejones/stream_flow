'use strict';

describe('myApp.flows module', function() {

  beforeEach(module('myApp.flows'));

  describe('flows controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var flowsCtrl = $controller('View1Ctrl');
      expect(flowsCtrl).toBeDefined();
    }));

  });
});